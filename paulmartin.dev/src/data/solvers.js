// People who solved the hidden maze. Temporary/manual list — Paul edits this by
// hand for now (no backend). Each entry: { name, note? }.
export const solvers = [
  // { name: 'First Finder', note: 'the very first' },
]
