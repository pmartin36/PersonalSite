{
  "name": "personalsite",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for Paul Martin's personal web properties.",
  "scripts": {
    "dev": "npm --prefix paulmartin.dev run dev",
    "build": "npm --prefix paulmartin.dev install && npm --prefix paulmartin.dev run build",
    "deploy": "npm run build && firebase deploy --only hosting",
    "deploy:paulmartin": "npm run build && firebase deploy --only hosting:paulmartin",
    "deploy:ulmartin": "firebase deploy --only hosting:ulmartin"
  },
  "devDependencies": {
    "firebase-tools": "^15.24.0"
  }
}
